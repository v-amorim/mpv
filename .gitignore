# Shader cache
cache/

# Shaders (large, not tracked - see shaders_list.txt)
*.glsl
*.hook

# Logs
*.log

# Tokens / secrets
anilistToken*.txt

portable_config/scripts/uosc/

# Python bytecode
__pycache__/

# Vendored python packages, recreated by `just setup`
vendor/

# An mpv unpacked into the repo root, so portable_config/ is read where it sits
# rather than copied. Everything a Windows build drops beside its executable.
/mpv.exe
/mpv.com
/*.dll
/mpv/
/doc/
/installer/
/updater.bat
/mpv-register.bat
/mpv-unregister.bat
CLAUDE.md
