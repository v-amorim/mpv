# ---------------------------------------------------------------------------
# [ESC] [F1] [F2] [F3] [F4] [F5] [F6] [F7] [F8] [F9] [F10] [F11] [F12]
# ---------------------------------------------------------------------------
ESC                 set                 fullscreen  no # Leaves fullscreen, without toggling back into it

F1                  show-text           ${playlist} # Lists the queued files as plain text
F2                  show-text           ${track-list} # Lists every audio and subtitle track the file carries
F3                  ignore
F4                  script-binding      sub-seek-list # Every subtitle line as a menu; type to filter, pick one to jump there
F5                  script-binding      restart-mpv # Reloads the file at the same position, applying config edits
F6                  script-binding      keybind-visualizer # This map: an on-screen keyboard of every binding, searchable
F7                  ignore
F8                  no-osd cycle-values sub-font "Roboto Medium" "Impress BT Pace" "Open Sans Semibold" "Noto Sans ExtraBold" ; expand-properties script-message-to osd_theme say "Subtitle font" "${sub-font}" "overrides the typeface the file asks for" # Steps through the four subtitle typefaces
F9                  script-binding      skip-to-silence # Jumps to the next silent gap, usually where the opening ends
F10                 script-binding      watched-folder # Moves the file to the watched folder once it finishes
F11                 script-binding      chapter-skip # Auto-skips chapters named as openings, endings, credits or previews
F12                 script-binding      export-selected-subtitles # Writes the active subtitle track next to the video file

SHIFT+F1            no-osd add          sub-delay    -0.05 ; expand-properties script-message-to osd_theme say "Subtitle delay" "${sub-delay}" "negative shows the line earlier, positive later" # Shows the subtitles earlier, in small steps
SHIFT+F2            no-osd add          sub-delay    +0.05 ; expand-properties script-message-to osd_theme say "Subtitle delay" "${sub-delay}" "negative shows the line earlier, positive later" # Shows the subtitles later, in small steps
SHIFT+F3            no-osd add          audio-delay  -0.010 ; expand-properties script-message-to osd_theme say "Audio delay" "${audio-delay}" "negative pulls the sound earlier, for lips out of sync" # Pulls the sound earlier when the lips run ahead of it
SHIFT+F4            no-osd add          audio-delay  +0.010 ; expand-properties script-message-to osd_theme say "Audio delay" "${audio-delay}" "positive pushes the sound later, for lips out of sync" # Pushes the sound later when the lips lag behind it
SHIFT+F5            no-osd set          sub-delay    0.0; no-osd set audio-delay 0.0; script-message-to osd_theme say "Reset" "sync" "subtitle and audio delay back to zero" # Puts both the subtitle and audio delay back to zero

ALT+F5              script-binding      reset-all # Zoom, pan, speed, delays, subtitles and colours back to defaults


# ---------------------------------------------------------------------------
# ['] [1] [2] [3] [4] [5] [6] [7] [8] [9] [0] [-] [=] [BS]
# ["] [!] [@] [#] [$] [%] [¨] [&] [*] [(] [)] [_] [+]
# ---------------------------------------------------------------------------
1                   no-osd add          contrast    -1 ; expand-properties script-message-to osd_theme say "Contrast" "${contrast}" "distance between the darkest and brightest parts" # Flattens the gap between the darkest and brightest parts
2                   no-osd add          contrast    +1 ; expand-properties script-message-to osd_theme say "Contrast" "${contrast}" "distance between the darkest and brightest parts" # Widens the gap between the darkest and brightest parts
3                   no-osd add          brightness  -1 ; expand-properties script-message-to osd_theme say "Brightness" "${brightness}" "lifts or drops the whole picture at once" # Darkens the whole picture at once
4                   no-osd add          brightness  +1 ; expand-properties script-message-to osd_theme say "Brightness" "${brightness}" "lifts or drops the whole picture at once" # Lightens the whole picture at once
5                   no-osd add          gamma       -1 ; expand-properties script-message-to osd_theme say "Gamma" "${gamma}" "opens up the shadows without washing out the highlights" # Deepens the shadows, leaving the highlights alone
6                   no-osd add          gamma       +1 ; expand-properties script-message-to osd_theme say "Gamma" "${gamma}" "opens up the shadows without washing out the highlights" # Opens up the shadows, leaving the highlights alone
7                   no-osd add          saturation  -1 ; expand-properties script-message-to osd_theme say "Saturation" "${saturation}" "how strong the colours are, far down is black and white" # Drains the colour, far enough down is black and white
8                   no-osd add          saturation  +1 ; expand-properties script-message-to osd_theme say "Saturation" "${saturation}" "how strong the colours are, far down is black and white" # Strengthens the colour
9                   no-osd add          volume      -2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume down
0                   no-osd add          volume      +2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume up
Alt+0               no-osd set          contrast    0; no-osd set brightness 0; no-osd set gamma 0; no-osd set saturation 0; no-osd change-list glsl-shaders clr ""; script-message-to osd_theme say "Reset" "picture" "contrast, brightness, gamma, saturation and shaders back to default" # Colour controls and shaders back to default
-                   no-osd add          sub-scale   -0.1 ; expand-properties script-message-to osd_theme say "Subtitle size" "${sub-scale}" "1.0 is the size the config asks for" # Shrinks the subtitles
CTRL+-              no-osd add          audio-delay -0.100 ; expand-properties script-message-to osd_theme say "Audio delay" "${audio-delay}" "coarse step, negative pulls the sound earlier" # Pulls the sound earlier, in coarse steps
=                   no-osd add          sub-scale   +0.1 ; expand-properties script-message-to osd_theme say "Subtitle size" "${sub-scale}" "1.0 is the size the config asks for" # Grows the subtitles
CTRL+=              no-osd add          audio-delay +0.100 ; expand-properties script-message-to osd_theme say "Audio delay" "${audio-delay}" "coarse step, positive pushes the sound later" # Pushes the sound later, in coarse steps
BS                  no-osd set          speed       +1.0 ; script-message-to osd_theme say "Speed" "1.00" "back to normal playback speed" # Playback speed back to normal

# ALT+BS              set                 video-zoom  +0
# SHIFT+BS            revert-seek
# SHIFT+CTRL+BS       revert-seek         mark
# !                   ignore
# @                   ignore
# SHARP               ignore
# $                   ignore
# %                   ignore
# ¨                   ignore
# &                   ignore
# *                   ignore
# )                   ignore
# (                   ignore
# _                   ignore
# +                   ignore

ALT+1 no-osd af toggle "lavfi=[loudnorm=i=-20]" ; script-message-to osd_theme say "Quiet levelling" "toggled" "holds the whole file near one loudness" # Quiet levelling: holds the whole file near one loudness
ALT+2 no-osd af toggle "dynaudnorm=g=5:f=250:r=0.9:p=0.5" ; script-message-to osd_theme say "Gentle auto volume" "toggled" "lifts quiet moments as they come" # Gentle auto volume: lifts quiet moments as they come
ALT+3 no-osd af toggle "acompressor=ratio=4,loudnorm" ; script-message-to osd_theme say "Compressor" "toggled" "squashes loud effects so speech stops hiding under them" # Compressor: squashes loud effects so speech stops hiding
ALT+4 no-osd af toggle "lavfi=[alimiter=10:1:1:5:8000]" ; script-message-to osd_theme say "Limiter" "toggled" "caps the peaks so explosions stop startling you" # Limiter: caps the peaks so explosions stop startling you
ALT+5 no-osd af toggle "lavfi=[dynaudnorm=g=3:f=250:r=0.9:p=0.9:m=10]" ; script-message-to osd_theme say "Aggressive auto volume" "toggled" "flattens loud and quiet hard, at the cost of punch" # Aggressive auto volume: flattens loud and quiet hard
ALT+6 no-osd af toggle 'pan="stereo|FL=0.707*FC+0.3*FL+0.1*BL+0.1*LFE|FR=0.707*FC+0.3*FR+0.1*BR+0.1*LFE"' ; script-message-to osd_theme say "Surround downmix" "toggled" "folds 5.1 into stereo with the dialogue pushed forward" # Surround downmix: 5.1 into stereo, dialogue pushed forward
ALT+7 no-osd af toggle "lavfi=[loudnorm=i=-14.0:lra=13.0:tp=-1.0]" ; script-message-to osd_theme say "Streaming loudness" "toggled" "louder than ALT+1 and keeps more of the dynamics" # Streaming loudness: louder than ALT+1, keeps more dynamics


# ---------------------------------------------------------------------------
# [q] [w] [e] [r] [t] [y] [u] [i] [o] [p] [´] [[]
# [Q] [W] [E] [R] [T] [Y] [U] [I] [O] [P] [`] [{]
# ---------------------------------------------------------------------------
q                   quit # Closes the player, forgetting where you were
SHIFT+q             quit-watch-later # Closes the player, resuming here next time

w                   no-osd add          panscan  -0.1 ; expand-properties script-message-to osd_theme say "Pan and scan" "${panscan}" "crops the black bars away by cutting the sides off" # Gives the black bars back
SHIFT+w             no-osd add          panscan  +0.1 ; expand-properties script-message-to osd_theme say "Pan and scan" "${panscan}" "crops the black bars away by cutting the sides off" # Crops the black bars away, cutting the sides off
CTRL+w              quit # Closes the player, forgetting where you were

e                   script-binding      uosc/editions # Picks between the cuts a file carries, such as theatrical or extended
SHIFT+e             ignore

r                   ignore
SHIFT+r             ignore
CTRL+r              sub-reload # Re-reads the subtitle file, picking up edits made while playing

t                   ignore
SHIFT+t             no-osd cycle        ontop ; expand-properties script-message-to osd_theme say "Always on top" "${ontop}" "keeps the window above every other one" # Keeps the window above every other one

y                   ignore
SHIFT+y             ignore

u                   no-osd cycle-values sub-ass-override "force" "yes" ; expand-properties script-message-to osd_theme say "Subtitle styling" "${?=sub-ass-override==force:my font and colours}${?=sub-ass-override==yes:as the file asks}" "styled subtitles carry their own look, this decides who wins" # Whether my font wins over the one a styled subtitle names
SHIFT+u             no-osd cycle        sub-forced-events-only ; expand-properties script-message-to osd_theme say "Forced lines only" "${sub-forced-events-only}" "shows just the signs and foreign speech, not the dialogue" # Shows just the signs and foreign speech, not the dialogue

i                   script-binding      stats/display-stats # Codec, resolution, frame timing and cache, while held
SHIFT+i             script-binding      stats/display-stats-toggle # Pins that same readout on screen until pressed again

o                   show-progress; script-binding uosc/flash-progress # Flashes the timeline and elapsed time
SHIFT+o             no-osd cycle-values osd-level 3 1; script-binding uosc/toggle-progress; expand-properties script-message-to osd_theme say "Timer" "${?=osd-level==3:pinned}${?=osd-level==1:hidden}" "keeps the elapsed time on screen instead of flashing it" # Pins the elapsed time on screen instead of flashing it
CTRL+o              script-binding      uosc/open-file # Browses the disk and plays a file, or drops it in the playlist

p                   script-binding      uosc/playlist # Everything queued right now; pick an entry to jump to it
SHIFT+p             ignore

`                   ignore
`                   ignore

[                   no-osd add          speed  -0.25 ; expand-properties script-message-to osd_theme say "Speed" "${speed}" "coarse step, pitch stays corrected" # Slows playback in coarse steps, pitch stays corrected
{                   no-osd add          speed  -0.05 ; expand-properties script-message-to osd_theme say "Speed" "${speed}" "fine step, pitch stays corrected" # Slows playback in fine steps, pitch stays corrected



# ---------------------------------------------------------------------------
# [a] [s] [d] [f] [g] [h] [j] [k] [l] [~] []]
# [A] [S] [D] [F] [G] [H] [J] [K] [L] [^] [}]
# ---------------------------------------------------------------------------
a                   no-osd cycle        audio ; expand-properties script-message-to osd_theme say "Audio track" "${?current-tracks/audio/lang:${current-tracks/audio/lang}}${!current-tracks/audio/lang:${aid}}" "language or commentary the film is playing" # Next audio track: another language or a commentary
SHIFT+a             no-osd cycle        audio down ; expand-properties script-message-to osd_theme say "Audio track" "${?current-tracks/audio/lang:${current-tracks/audio/lang}}${!current-tracks/audio/lang:${aid}}" "language or commentary the film is playing" # Previous audio track
CTRL+a              script-binding      uosc/audio # Audio tracks as a menu, rather than cycling blind

s                   no-osd cycle        sub ; expand-properties script-message-to osd_theme say "Subtitle track" "${?current-tracks/sub/lang:${current-tracks/sub/lang}}${!current-tracks/sub/lang:${sid}}" "cycles the tracks the file carries, ending on off" # Next subtitle track, ending on off
SHIFT+s             no-osd cycle        sub down ; expand-properties script-message-to osd_theme say "Subtitle track" "${?current-tracks/sub/lang:${current-tracks/sub/lang}}${!current-tracks/sub/lang:${sid}}" "cycles the tracks the file carries, ending on off" # Previous subtitle track
CTRL+s              script-binding      uosc/subtitles # Subtitle tracks as a menu, rather than cycling blind
ALT+s               screenshot          window # Saves exactly what the window shows, at window size

d                   no-osd cycle        deinterlace ; expand-properties script-message-to osd_theme say "Deinterlace" "${deinterlace}" "combs out the horizontal tearing old TV video shows on movement" # Combs out the tearing old TV video shows on movement
SHIFT+d             no-osd cycle        deband ; expand-properties script-message-to osd_theme say "Debanding" "${deband}" "smooths the visible steps in skies and other flat gradients" # Smooths the visible steps in skies and other gradients
CTRL+d              no-osd set          deband yes; no-osd set deband-iterations 1; script-message-to osd_theme say "Debanding" "low" "one pass, the cheapest smoothing of gradient steps" # Debanding at one pass, the cheapest setting
CTRL+SHIFT+d        no-osd set          deband yes; no-osd set deband-iterations 2; script-message-to osd_theme say "Debanding" "medium" "two passes, for sources that still show rings" # Debanding at two passes, for sources that still ring
ALT+d               no-osd set          deband yes; no-osd set deband-iterations 3; script-message-to osd_theme say "Debanding" "high" "three passes, heaviest and softest on fine detail" # Debanding at three passes, heaviest on fine detail

f                   cycle               fullscreen # Enters or leaves fullscreen
SHIFT+f             script-binding      display-fps-toggle # Shows how many frames the display is actually drawing

g                   no-osd cycle        interpolation ; expand-properties script-message-to osd_theme say "Interpolation" "${interpolation}" "resamples frames to the screen rate so panning stops juddering" # Resamples frames to the screen rate, killing pan judder
SHIFT+g             ignore

h                   ignore
SHIFT+h             ignore
CTRL+h              no-osd set          target-trc bt.1886; script-message-to osd_theme say "Display output" "SDR" "tone the picture for an ordinary screen" # Tones the picture for an ordinary screen
CTRL+SHIFT+h        no-osd set          target-trc pq; script-message-to osd_theme say "Display output" "HDR" "send the wide brightness range an HDR screen expects" # Sends the wide brightness range an HDR screen expects
# CTRL+h              cycle-values        hwdec "auto-safe" "no"

j                   seek                -86 exact exact # Jumps back roughly one opening, for when it skipped too far
SHIFT+j             ignore

k                   playlist-shuffle    ; script-message-to osd_theme say "Playlist" "shuffled" "the queue is reordered at random from here on" # Reorders the queue at random from here on
SHIFT+k             playlist-unshuffle  ; script-message-to osd_theme say "Playlist" "unshuffled" "back to the order the files were loaded in" # Back to the order the files were loaded in

l                   seek                +86 exact # Skips roughly one opening
CTRL+l              seek                +177 exact # Skips roughly one opening plus an ending
SHIFT+l             no-osd cycle-values loop-file yes no ; expand-properties script-message-to osd_theme say "Loop" "${loop-file}" "replays this file forever instead of moving to the next" # Replays this file forever instead of moving to the next


]                   no-osd add          speed  +0.25 ; expand-properties script-message-to osd_theme say "Speed" "${speed}" "coarse step, pitch stays corrected" # Speeds playback in coarse steps, pitch stays corrected
}                   no-osd add          speed  +0.05 ; expand-properties script-message-to osd_theme say "Speed" "${speed}" "fine step, pitch stays corrected" # Speeds playback in fine steps, pitch stays corrected


# ---------------------------------------------------------------------------
# [\] [z] [x] [c] [v] [b] [n] [m] [,] [.] [;] [/]
# [|] [Z] [X] [C] [V] [B] [N] [M] [<] [>] [:] [?]
# ---------------------------------------------------------------------------
z                   no-osd add          chapter +1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps to the next marked section of the file" # Jumps to the next marked section of the file
SHIFT+z             no-osd add          chapter -1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps back to the previous marked section" # Jumps back to the previous marked section

x                   screenshot          video # Saves the raw frame at source resolution, without subtitles
SHIFT+x             screenshot          video each-frame # Saves one raw file per frame until pressed again

c                   script-binding      uosc/chapters # Every chapter in the file; pick one to jump there
SHIFT+c             async               screenshot # Saves the frame as shown, subtitles and all, without stuttering
CTRL+c              script-binding      clipshot-subs # Copies the subtitle line playing right now to the clipboard

v                   no-osd cycle        sub-visibility ; expand-properties script-message-to osd_theme say "Subtitles" "${sub-visibility}" "hides the text without losing which track is selected" # Hides the text without losing which track is selected
SHIFT+v             no-osd cycle        sub-ass-use-video-data ; expand-properties script-message-to osd_theme say "Subtitle stretch fix" "${sub-ass-use-video-data}" "reads the video shape to undo stretched or squashed styled text" # Undoes stretched or squashed styled subtitles
CTRL+v              script-binding      uosc/video # Switches between the video tracks the file carries
ALT+v               no-osd cycle        secondary-sub-visibility ; expand-properties script-message-to osd_theme say "Second subtitles" "${secondary-sub-visibility}" "the extra track shown at the top, for a second language" # The extra track at the top, for a second language

b                   no-osd af toggle    "lavfi=[loudnorm=I=-14:TP=-3:LRA=4]" ; script-message-to osd_theme say "Dialogue boost" "toggled" "levels the track so speech stops hiding under the effects" # Levels the track so speech stops hiding under the effects
SHIFT+b             ignore

n                   script-binding      update_anilist # Marks the episode watched on AniList
SHIFT+n             script-binding      launch_anilist # Opens the AniList page for what is playing

m                   no-osd cycle        mute ; expand-properties script-message-to osd_theme say "Mute" "${mute}" "" # Silences the audio without moving the volume
SHIFT+m             ignore

,                   frame-back-step # Steps back exactly one frame
<                   no-osd cycle-values play-dir - + ; expand-properties script-message-to osd_theme say "Playback" "${?=play-dir==forward:forwards}${?=play-dir==backward:backwards}" "reverse playback, heavy on the machine and rough on long files" # Plays backwards, heavy on the machine and rough on long files

.                   frame-step # Steps forward exactly one frame
>                   ignore

/                   no-osd set speed    +1.0 ; script-message-to osd_theme say "Speed" "1.00" "back to normal playback speed" # Playback speed back to normal
?                   script-binding      console/enable # Type mpv commands and see what they return


# ---------------------------------------------------------------------------
# Arrow Keys  [↑UP]
#     [←LEFT] [↓DOWN] [RIGHT→]
# ---------------------------------------------------------------------------
UP                  no-osd add          volume    +2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume up
DOWN                no-osd add          volume    -2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume down
LEFT                seek                -5; script-binding uosc/flash-timeline # Jumps back five seconds
RIGHT               seek                +5; script-binding uosc/flash-timeline # Jumps forward five seconds

SHIFT+UP            no-osd              seek      +5 exact # Jumps forward five seconds, landing on the exact frame
SHIFT+DOWN          no-osd              seek      -5 exact # Jumps back five seconds, landing on the exact frame
SHIFT+LEFT          no-osd add          chapter   -1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps back to the previous marked section" # Jumps back to the previous marked section
SHIFT+RIGHT         no-osd add          chapter   +1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps to the next marked section of the file" # Jumps to the next marked section of the file

ALT+UP              no-osd add          video-pan-y  +0.1 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-y}" "slides a zoomed picture around inside the window" # Slides a zoomed picture down inside the window
ALT+DOWN            no-osd add          video-pan-y  -0.1 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-y}" "slides a zoomed picture around inside the window" # Slides a zoomed picture up inside the window
ALT+LEFT            no-osd add          video-pan-x  +0.1 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-x}" "slides a zoomed picture around inside the window" # Slides a zoomed picture right inside the window
ALT+RIGHT           no-osd add          video-pan-x  -0.1 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-x}" "slides a zoomed picture around inside the window" # Slides a zoomed picture left inside the window

CTRL+DOWN           no-osd              sub-seek  0 # Replays the subtitle line currently on screen
CTRL+RIGHT          no-osd              sub-seek  +1 # Jumps the video to the next subtitle line
CTRL+LEFT           no-osd              sub-seek  -1 # Jumps the video back to the previous subtitle line

CTRL+SHIFT+LEFT     no-osd              sub-step  -1 ; expand-properties script-message-to osd_theme say "Subtitle delay" "${sub-delay}" "shifted by one line, the video keeps playing" # Retimes the subtitles one line back, video keeps playing
CTRL+SHIFT+RIGHT    no-osd              sub-step  +1 ; expand-properties script-message-to osd_theme say "Subtitle delay" "${sub-delay}" "shifted by one line, the video keeps playing" # Retimes the subtitles one line forward, video keeps playing


# ---------------------------------------------------------------------------
# [SPACE]   [ENTER] [TAB]
# [INSERT]  [HOME]  [PGUP]
# [DEL]     [END]   [PGDOWN]
# [MENU]    [PAUSE]
# ---------------------------------------------------------------------------
SPACE               cycle               pause # Pauses or resumes playback

ALT+ENTER           cycle               fullscreen # Enters or leaves fullscreen

TAB                 script-binding      uosc/toggle-ui # Shows or hides the whole uosc interface
SHIFT+TAB           script-binding      uosc/toggle-progress # Shows or hides the thin progress bar along the bottom

HOME                ignore
PGUP                playlist-prev # Plays the previous file in the queue
SHIFT+PGUP          no-osd add          chapter  -1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps back to the previous marked section" # Jumps back to the previous marked section

DEL                 ignore
END                 ignore
PGDWN               playlist-next # Plays the next file in the queue
SHIFT+PGDWN         no-osd add          chapter  +1 ; expand-properties script-message-to osd_theme say "Chapter" "${?chapter-metadata/title:${chapter-metadata/title}}${!chapter-metadata/title:${?chapter:${chapter}}${!chapter:none}}" "jumps to the next marked section of the file" # Jumps to the next marked section of the file

MENU                script-binding      uosc_menu/menu # Opens the main menu, centred

PAUSE               script-binding      stats/display-page-4 # The stats page holding the internal performance graphs

# ---------------------------------------------------------------------------
# Numpad
# [KP7]     [KP8]       [KP9]
# [KP4]     [KP5]       [KP6]
# [KP1]     [KP2]       [KP3]
# [KP0]     [KP_DEC]    [KP_ENTER]
# ---------------------------------------------------------------------------
KP0                 no-osd cycle-values video-rotate    90 180 270 0 ; expand-properties script-message-to osd_theme say "Rotation" "${video-rotate}" "turns the picture, for footage shot sideways" # Turns the picture a quarter at a time, for sideways footage
KP1                 no-osd add          video-zoom      -0.05 ; expand-properties script-message-to osd_theme say "Zoom" "${video-zoom}" "0 fits the window, above that the edges are cut off" # Zooms out
KP2                 no-osd add          video-pan-y     +0.01 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-y}" "slides a zoomed picture around inside the window" # Nudges a zoomed picture down
KP3                 no-osd add          video-zoom      +0.05 ; expand-properties script-message-to osd_theme say "Zoom" "${video-zoom}" "0 fits the window, above that the edges are cut off" # Zooms in, cutting the edges off
KP4                 no-osd add          video-pan-x     -0.01 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-x}" "slides a zoomed picture around inside the window" # Nudges a zoomed picture left
KP5                 no-osd set          video-zoom      +0.00 ; no-osd set video-pan-x 0 ; no-osd set video-pan-y 0 ; no-osd set video-aspect 0 ; script-message-to osd_theme say "Reset" "framing" "zoom, pan and aspect ratio back to default" # Zoom, pan and aspect ratio back to default
KP6                 no-osd add          video-pan-x     +0.01 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-x}" "slides a zoomed picture around inside the window" # Nudges a zoomed picture right
KP7                 no-osd add          video-zoom      -0.05 ; expand-properties script-message-to osd_theme say "Zoom" "${video-zoom}" "0 fits the window, above that the edges are cut off" # Zooms out
KP8                 no-osd add          video-pan-y     -0.01 ; expand-properties script-message-to osd_theme say "Pan" "${video-pan-y}" "slides a zoomed picture around inside the window" # Nudges a zoomed picture up
KP9                 no-osd add          video-zoom      +0.05 ; expand-properties script-message-to osd_theme say "Zoom" "${video-zoom}" "0 fits the window, above that the edges are cut off" # Zooms in, cutting the edges off

CTRL+KP1            no-osd cycle-values sub-pos         60 100; no-osd cycle-values sub-color "#80FFFFFF" "#FFFFFF" ; no-osd cycle-values sub-border-color "#80000000" "#000000" ; no-osd cycle-values sub-shadow-color "#80000000" "#000000" ; no-osd cycle-values sub-ass-override "force" "yes" ; expand-properties script-message-to osd_theme say "Subtitle preset" "${?=sub-pos==100:bottom, solid}${?=sub-pos==60:middle, dimmed}" "position and transparency together, for burnt in text underneath" # Swaps position and transparency together, for burnt in text
CTRL+KP2            no-osd set          sub-pos         100 ; script-message-to osd_theme say "Subtitle position" "bottom" "the usual place, along the bottom edge" # Subtitles along the bottom edge, the usual place
CTRL+KP4            no-osd set          sub-ass-style-overrides "PrimaryColour=&H80FFFFFF,FontName=Impress BT Pace" ; script-message-to osd_theme say "Subtitle look" "dimmed white" "half transparent, so burnt in text stays readable underneath" # Half transparent subtitles, so burnt in text stays readable
CTRL+KP5            no-osd set          sub-pos         60 ; script-message-to osd_theme say "Subtitle position" "middle" "lifted off the bottom, clear of burnt in text" # Subtitles lifted off the bottom, clear of burnt in text
CTRL+KP6            no-osd set          sub-ass-style-overrides "PrimaryColour=&HFFFFFF,FontName=Impress BT Pace" ; script-message-to osd_theme say "Subtitle look" "solid white" "fully opaque, the plain readable default" # Fully opaque subtitles, the plain readable default
CTRL+KP8            no-osd set          sub-pos         0 ; script-message-to osd_theme say "Subtitle position" "top" "moved to the top edge, out of the way of the picture" # Subtitles at the top edge, out of the way of the picture

ALT+KP2             no-osd add          sub-pos         +1 ; expand-properties script-message-to osd_theme say "Subtitle position" "${sub-pos}" "0 is the top edge, 100 the bottom" # Nudges the subtitles down a line
ALT+KP5             no-osd cycle-values video-aspect-override "16:9" "4:3" "2.35:1" "-1" ; expand-properties script-message-to osd_theme say "Aspect ratio" "${video-aspect-override}" "forces the picture shape when the file lies about it, -1 trusts the file" # Forces the picture shape when the file lies about it
ALT+KP8             no-osd add          sub-pos         -1 ; expand-properties script-message-to osd_theme say "Subtitle position" "${sub-pos}" "0 is the top edge, 100 the bottom" # Nudges the subtitles up a line

KP_DEC              ignore
KP_ENTER            ignore

CTRL+ALT+ç          script-binding      screenshot2 # Saves a screenshot through the secondary screenshot binding


# ---------------------------------------------------------------------------
#  Mouse
#  [MBTN_LEFT/_DBL] [WHEEL_UP/WHEEL_DOWN] [MBTN_RIGHT/_DBR]
#  [MBTN_BACK]      [MBTN_FORWARD]
# ---------------------------------------------------------------------------
MBTN_LEFT           cycle               pause # Pauses or resumes playback
MBTN_LEFT_DBL       cycle               fullscreen # Enters or leaves fullscreen

MBTN_RIGHT          script-binding      uosc_menu/menu-at-cursor # Opens the main menu where the pointer is

MBTN_BACK           playlist-prev # Plays the previous file in the queue
MBTN_FORWARD        playlist-next # Plays the next file in the queue

WHEEL_UP            no-osd add          volume  +2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume up
WHEEL_DOWN          no-osd add          volume  -2 ; expand-properties script-message-to osd_theme say "Volume" "${volume}" "" # Turns the volume down

CTRL+WHEEL_UP       seek                +5 exact # Jumps forward five seconds
CTRL+WHEEL_DOWN     seek                -5 exact # Jumps back five seconds


# ---------------------------------------------------------------------------
# Anime4K Shaders (Low end)
# ---------------------------------------------------------------------------
CTRL+1              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode A (Fast)" "restores detail then upscales, for clean but soft drawn animation" # Anime4K Mode A (Fast): restores detail then upscales, for clean but soft drawn animation
CTRL+2              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode B (Fast)" "gentler restore, for blurry or worn out sources" # Anime4K Mode B (Fast): gentler restore, for blurry or worn out sources
CTRL+3              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode C (Fast)" "denoises while it upscales, for noisy or heavily compressed sources" # Anime4K Mode C (Fast): denoises while it upscales, for noisy or heavily compressed sources
CTRL+4              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_S.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode A+A (Fast)" "restores detail then upscales, for clean but soft drawn animation" # Anime4K Mode A+A (Fast): restores detail then upscales, for clean but soft drawn animation
CTRL+5              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_S.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode B+B (Fast)" "gentler restore, for blurry or worn out sources" # Anime4K Mode B+B (Fast): gentler restore, for blurry or worn out sources
CTRL+6              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_S.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_S.glsl"; script-message-to osd_theme say "Anime4K" "Mode C+A (Fast)" "denoises while it upscales, for noisy or heavily compressed sources" # Anime4K Mode C+A (Fast): denoises while it upscales, for noisy or heavily compressed sources
CTRL+7              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl;~~/shaders/adaptive-sharpen.glsl"; script-message-to osd_theme say "Anime4K" "Mode A (HQ) + Sharpen" "restores detail then upscales, for clean but soft drawn animation" # Anime4K Mode A (HQ) + Sharpen: restores detail then upscales, for clean but soft drawn animation
CTRL+0              no-osd              change-list glsl-shaders clr ""; script-message-to osd_theme say "Shaders" "cleared" "back to plain output, no GPU upscaling" # Drops every shader, back to plain output

# ---------------------------------------------------------------------------
# Anime4K Shaders (High end)
# ---------------------------------------------------------------------------
CTRL+!              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode A (HQ)" "restores detail then upscales, for clean but soft drawn animation" # Anime4K Mode A (HQ): restores detail then upscales, for clean but soft drawn animation
CTRL+@              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode B (HQ)" "gentler restore, for blurry or worn out sources" # Anime4K Mode B (HQ): gentler restore, for blurry or worn out sources
CTRL+SHARP          no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode C (HQ)" "denoises while it upscales, for noisy or heavily compressed sources" # Anime4K Mode C (HQ): denoises while it upscales, for noisy or heavily compressed sources
CTRL+$              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_M.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode A+A (HQ)" "restores detail then upscales, for clean but soft drawn animation" # Anime4K Mode A+A (HQ): restores detail then upscales, for clean but soft drawn animation
CTRL+%              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_Soft_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode B+B (HQ)" "gentler restore, for blurry or worn out sources" # Anime4K Mode B+B (HQ): gentler restore, for blurry or worn out sources
CTRL+¨              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_VL.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x2.glsl;~~/shaders/Anime4K/Anime4K_AutoDownscalePre_x4.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_M.glsl;~~/shaders/Anime4K/Anime4K_Upscale_CNN_x2_M.glsl"; script-message-to osd_theme say "Anime4K" "Mode C+A (HQ)" "denoises while it upscales, for noisy or heavily compressed sources" # Anime4K Mode C+A (HQ): denoises while it upscales, for noisy or heavily compressed sources
CTRL+&              no-osd              change-list glsl-shaders set "~~/shaders/Anime4K/Anime4K_Clamp_Highlights.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_UL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_UL.glsl;~~/shaders/Anime4K/Anime4K_Restore_CNN_VL.glsl;~~/shaders/Anime4K/Anime4K_Upscale_Denoise_CNN_x2_UL.glsl;~~/shaders/Anime4K/Anime4K_Thin_HQ.glsl;~~/shaders/adaptive-sharpen.glsl"; script-message-to osd_theme say "Anime4K" "High GPU A+A UL+VL No Downscale + Thinlines & UpscaleDenoise Instead of Upscale" "restores detail then upscales, for clean but soft drawn animation" # Anime4K High GPU A+A UL+VL No Downscale + Thinlines & UpscaleDenoise Instead of Upscale: restores detail then upscales, for clean but soft drawn animation

# ---------------------------------------------------------------------------
# UOSC Menu Config
# ---------------------------------------------------------------------------
# The tree below is read by scripts/uosc-menu.lua, which hands it to uosc with
# the icons applied. "@name" picks a Material Icons Rounded ligature from
# fonts/uosc_icons.otf: https://fonts.google.com/icons?icon.style=Rounded
# "?text" describes the entry, shown under the menu while it is hovered.
# Entries that carry a state draw a checkbox or a radio button instead of their
# icon, reflecting mpv when the menu opens: "cycle <prop>", "af toggle <filter>"
# and "change-list glsl-shaders toggle <file>" are checkboxes, "set <prop>
# <value>" is a radio button among the entries that set the same property.
# A "---" part draws a separator under the last entry of the level it sits in,
# so it goes one level up from the entry it follows: "Video > ---", never
# "Video > Video Track > ---".

CTRL+o script-binding    uosc/open-file                                                                #! Open File @file_open ?Browse the disk and play a file, or drop it in the playlist
# script-binding    uosc/show-in-directory                                                             #! Show in Explorer @folder_open ?Open the folder holding the current file, with it selected
p script-binding    uosc/playlist                                                                      #! Playlist @playlist_play ?Everything queued right now; pick an entry to jump to it
#                                                                                                      #! ---

# --- Video -----------------------------------------------------------------
g no-osd cycle interpolation ; expand-properties script-message-to osd_theme say "Interpolation" "${interpolation}" "resamples frames to the screen rate so panning stops juddering" #! Video @movie > Interpolation @animation ?Resample frames to the display rate, killing judder on panning shots
CTRL+v script-binding    uosc/video                                                                    #! Video > Video Track @video_library ?Switch between the video tracks the file carries
#                                                                                                      #! Video > ---
# set               video-aspect-override "-1"                                                         #! Video > Aspect Ratio @aspect_ratio ?Override the shape of the picture when the file lies about it > Default @crop_free ?Trust the aspect ratio the file declares
# set               video-aspect-override "16:9"                                                       #! Video > Aspect Ratio > 16:9 @crop_16_9 ?Force widescreen, the shape of most modern video
# set               video-aspect-override "4:3"                                                        #! Video > Aspect Ratio > 4:3 @crop_5_4 ?Force the old TV shape, for pre-HD material
# set               video-aspect-override "2.35:1"                                                     #! Video > Aspect Ratio > 2.35:1 @crop_landscape ?Force cinemascope, for films squeezed into a 16:9 container

# change-list       glsl-shaders toggle ~~/shaders/adasharp.glsl                                       #! Video > Shaders @auto_awesome > Sharpen @deblur ?Recover fine detail on soft or low bitrate sources > Adaptive Sharpen @deblur ?Sharpens by local contrast, so flat areas stay free of ringing
# change-list       glsl-shaders toggle ~~/shaders/NVSharpen.glsl                                      #! Video > Shaders > Sharpen > NVSharpen @deblur ?NVIDIA Image Scaling sharpener, cheap and even handed
# change-list       glsl-shaders toggle ~~/shaders/CAS.glsl                                            #! Video > Shaders > Contrast Adaptive @deblur ?AMD CAS: strongest where the picture is flat, gentle on edges
# change-list       glsl-shaders toggle ~~/shaders/adasharpA.glsl                                      #! Video > Shaders > Line Art @brush ?Tuned for drawn material rather than camera footage > Sharpen @deblur ?Adaptive sharpen retuned for flat cel shaded colour
# change-list       glsl-shaders toggle ~~/shaders/A4K_Thin.glsl                                       #! Video > Shaders > Line Art > Thin Lines @brush ?Anime4K line thinning, undoes the fattening that upscaling causes
# change-list       glsl-shaders toggle ~~/shaders/A4K_Dark.glsl                                       #! Video > Shaders > Line Art > Darken Lines @brush ?Anime4K line darkening, restores outlines washed out by compression
# change-list       glsl-shaders toggle ~~/shaders/F16.glsl                                            #! Video > Shaders > Neural Scaler @memory ?Trained upscalers: the best results and the heaviest on the GPU > FSRCNNX 16 @memory ?Heavy CNN upscaler, the sharpest option for live action
# change-list       glsl-shaders toggle ~~/shaders/ravu_L_ar_r4.hook                                   #! Video > Shaders > Neural Scaler > Ravu Lite ar r4 @memory ?Light trained upscaler with anti-ringing, a good default
# change-list       glsl-shaders toggle ~~/shaders/ravu_Z_ar_r3.hook ; no-osd set cscale-antiring 0 ; set dscale-antiring 0 ; set cscale-antiring 0 #! Video > Shaders > Neural Scaler > Ravu Zoom ar r3 @memory ?Scales to any factor, not just doubling; disables the built in anti-ringing
# change-list       glsl-shaders toggle ~~/shaders/F16_LA.glsl                                         #! Video > Shaders > Neural Scaler > FSRCNNX Line Art @memory ?FSRCNNX retrained on drawn material
# change-list       glsl-shaders toggle ~~/shaders/A4K_Upscale_L.glsl                                  #! Video > Shaders > Neural Scaler > Anime4K Upscale L @memory ?Anime4K large upscaler, built for anime above all
# change-list       glsl-shaders toggle ~~/shaders/ssimsr.glsl                                         #! Video > Shaders > Generic Scaler @photo_size_select_large ?Maths based resamplers, cheap and source agnostic > SSimSuperRes @photo_size_select_large ?Upscales so a downscale of the result matches the source again
# change-list       glsl-shaders toggle ~~/shaders/CfL_P.glsl                                          #! Video > Shaders > Generic Scaler > CfL Prediction @photo_size_select_large ?Rebuilds chroma from luma, sharpening colour edges on 4:2:0 video
# change-list       glsl-shaders toggle ~~/shaders/ssimds.glsl ; no-osd set linear-downscaling no      #! Video > Shaders > Generic Scaler > SSimDownscaler @photo_size_select_large ?Downscaling that keeps perceived sharpness; needs linear downscaling off
# change-list       glsl-shaders toggle ~~/shaders/FSR_EASU.glsl                                       #! Video > Shaders > Generic Scaler > AMD FidelityFX EASU @photo_size_select_large ?The upscaling half of FSR, edge aware and very cheap
#                                                                                                      #! Video > Shaders > ---
# cycle-values      glsl-shaders "~~/shaders/nlmeans_HQ.glsl" "~~/shaders/nlmeans_L_HQ.glsl" ""        #! Video > Shaders > Denoise @grain ?Cycles NLMeans high quality, its lighter variant, then off
# change-list       glsl-shaders clr all                                                               #! Video > Shaders > Clear All @delete_sweep ?Drop every shader loaded above and go back to plain output

# apply-profile     Animation                                                                          #! Video > Profiles @tune ?Swap the whole rendering setup in one go > Animation @auto_awesome_motion ?The settings this config applies to files under an Animation folder
# apply-profile     fast                                                                               #! Video > Profiles > Fast @bolt ?mpv's low cost preset, for weak hardware or battery
# apply-profile     high-quality                                                                       #! Video > Profiles > High Quality @diamond ?mpv's demanding preset: better scaling, dithering and debanding

# --- Audio -----------------------------------------------------------------
CTRL+a script-binding    uosc/audio                                                                    #! Audio @volume_up > Audio Track @library_music ?Switch language or commentary track
# script-binding    uosc/audio-device                                                                  #! Audio > Output Device @speaker ?Send sound to another output without restarting playback
#                                                                                                      #! Audio > ---
# af                toggle "lavfi=[loudnorm=I=-14:TP=-3:LRA=4]" ; show-text "${af}"                    #! Audio > Boost Dialogue Volume @record_voice_over ?Levels loudness so quiet speech stops hiding under the effects
# cycle             audio-normalize-downmix                                                            #! Audio > Toggle Normalize @graphic_eq ?Evens out the volume when folding surround down to stereo
# af                clr ""                                                                             #! Audio > Clear Filters @filter_alt_off ?Remove every audio filter and hear the track untouched

# --- Subtitle --------------------------------------------------------------
CTRL+s              script-binding uosc/subtitles                                                      #! Subtitle @subtitles > Subtitle Track @closed_caption ?Pick which subtitle track shows, or turn them off
#                   script-binding uosc/load-subtitles                                                 #! Subtitle > Load Subtitle @upload_file ?Add a subtitle file from disk to this playback
#                                                                                                      #! Subtitle > ---
F4                  script-binding sub-seek-list                                                       #! Subtitle > Seek to Line @manage_search ?Lists every subtitle line; pick one to jump straight to it
CTRL+r              sub-reload                                                                         #! Subtitle > Reload External @refresh ?Re-read the subtitle file, picking up edits made while playing
F12 script-binding  export-selected-subtitles                                                          #! Subtitle > Export Selected @save_alt ?Write the active subtitle track next to the video file
#                                                                                                      #! Subtitle > ---
F8 no-osd cycle-values sub-font "Roboto Medium" "Impress BT Pace" "Open Sans Semibold" "Noto Sans ExtraBold" ; expand-properties script-message-to osd_theme say "Subtitle font" "${sub-font}" "overrides the typeface the file asks for" #! Subtitle > Font @text_fields ?Override the typeface the subtitles ask for > Cycle @loop ?Step through the four fonts below, one press at a time
#                   script-message-to sub_font use-file-font                                           #! Subtitle > Font > As the File Asks @restart_alt ?Drop the override so an ASS script uses the font it names
#                                                                                                      #! Subtitle > Font > ---
#                   set sub-font "Roboto Medium"                                                       #! Subtitle > Font > Roboto Medium @font_download ?Neutral and quiet, the safe pick for anything
#                   set sub-font "Impress BT Pace"                                                     #! Subtitle > Font > Impress BT Pace @font_download ?Condensed, fits long lines without shrinking them
#                   set sub-font "Open Sans Semibold"                                                  #! Subtitle > Font > Open Sans Semibold @font_download ?Rounder and heavier, easy to read from the couch
#                   set sub-font "Noto Sans ExtraBold"                                                 #! Subtitle > Font > Noto Sans ExtraBold @font_download ?The heaviest option, for bright scenes that swallow text

# --- Chapters --------------------------------------------------------------
c script-binding    uosc/chapters                                                                      #! Chapters @bookmarks > Chapter List @list ?Every chapter in the file; pick one to jump there
F11 script-binding  chapter-skip                                                                       #! Chapters > Toggle Chapter Skipping @skip_next ?Auto-skip chapters named as openings, endings, credits or previews
F9  script-binding  skip-to-silence                                                                    #! Chapters > Skip to Silence @fast_forward ?Jump to the next silent gap, usually where the opening ends
#                                                                                                      #! Chapters > ---
# script-binding chapters/add_chapter                                                                  #! Chapters > Add Chapter @add ?Mark the current position as the start of a new chapter
# script-binding chapters/remove_chapter                                                               #! Chapters > Remove Chapter @remove ?Delete the chapter playing right now
# script-binding chapters/edit_chapter                                                                 #! Chapters > Edit Chapter @edit ?Rename the chapter playing right now
#                                                                                                      #! Chapters > ---
# script-binding chapters/write_chapters                                                               #! Chapters > Write Chapters @save ?Save the edits to a chapter file beside the video
# script-binding chapters/write_txt                                                                    #! Chapters > Write Chapters with Timestamps @description ?Export the list as plain text, timestamps included
# script-binding chapters/write_xml                                                                    #! Chapters > Write MKV Chapters (XML) @code ?Export as Matroska XML, ready for mkvpropedit
# script-binding chapters/bake_chapters                                                                #! Chapters > Save Video with Chapters @movie ?Remux the video with the chapters baked into a new file
# script-binding chapters/mkvpropedit                                                                  #! Chapters > Edit MKV Chapters with mkvpropedit @construction ?Write the chapters into the MKV in place, no remux
#                                                                                                      #! Chapters > ---

# --- Screenshot ------------------------------------------------------------
# async             screenshot                                                                         #! Screenshot @photo_camera > Normal @photo_camera ?Saves the frame as shown, subtitles and OSD included
ALT+s screenshot    window                                                                             #! Screenshot > Window @screenshot_monitor ?Saves exactly what the window shows, at window size
# screenshot        each-frame                                                                         #! Screenshot > Each Frame @burst_mode ?Keeps saving one file per frame until pressed again
x screenshot        video                                                                              #! Screenshot > Video @videocam ?Saves the raw frame at source resolution, without subtitles
SHIFT+x screenshot        video each-frame                                                                   #! Screenshot > Video Each Frame @video_camera_back ?Raw frames, one file each, until pressed again

# --- Tools -----------------------------------------------------------------
SHIFT+i script-binding  stats/display-stats-toggle                                                         #! Tools @build > File Stats @analytics ?Pins the codec, resolution, frame timing and cache readout on screen
F10 script-binding  watched-folder                                                                     #! Tools > Toggle File Moving @drive_file_move ?Move the file to the watched folder once it finishes
#                                                                                                      #! Tools > ---
F6  script-binding  keybind-visualizer                                                                 #! Tools > Keyboard Map @keyboard ?On-screen keyboard showing what every key does; type to search
# script-binding    uosc/keybinds                                                                      #! Tools > Key Bindings @list_alt ?uosc's plain list of every active binding
? script-binding    console/enable                                                                     #! Tools > Console @terminal ?Type mpv commands and see what they return
#                                                                                                      #! Tools > ---
# script-binding    uosc/open-config-directory                                                         #! Tools > Open Config Folder @folder_open ?Open portable_config, where these settings live
# script-binding    uosc/update                                                                        #! Tools > Update uosc @update ?Fetch the newest uosc; local patches need patches/apply.sh afterwards
#                                                                                                      #! Tools > ---
F5  script-binding  restart-mpv                                                                        #! Tools > Restart mpv @restart_alt ?Reload the file at the same position, applying config edits
ALT+F5 script-binding reset-all                                                                        #! Tools > Reset All Settings @settings_backup_restore ?Zoom, pan, speed, delays, subtitles and colours back to defaults
